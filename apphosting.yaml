# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
  # You can specify environment variables for your backend. These variables are
  # available at build time and in your backend's runtime environment.
  # https://firebase.google.com/docs/app-hosting/configure#environment-variables
  environmentVariables:
    NAME: "world"
  # You can also specify secrets, which are a safer way to store sensitive
  # information such as API keys.
  # https://firebase.google.com/docs/app-hosting/configure#secrets
  secrets: [GEMINI_API_KEY]
